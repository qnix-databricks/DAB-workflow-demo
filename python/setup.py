from setuptools import setup, find_packages

setup(
    name="charter_utils",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[],
    description="Shared utilities for Charter Databricks workflows",
    author="quan.ta",
)
